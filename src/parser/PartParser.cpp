#include "PartParser.hpp"

PartParser::PartParser( void )
{

}

PartParser::~PartParser( void )
{

}

int	PartParser::CmdParser( void )
{
	if (tokens.size() < 2) // The variable must be number 5
		throw (461);
	return (0);
}